// 自定义layer弹框样式
layer.config({
    skin: 'self-iframe'
})
layer.config({
    skin: 'self'
})
layer.config({
    skin: 'self-success'
})

// 使用方式
// 引入layer.js，引入jQuery
//引入common.css

// 引入layer-custom.js   或者直接把上面的定义方式拷到自己的页面

// 在需要用的地方直接把皮肤设置成
// skin: 'self'